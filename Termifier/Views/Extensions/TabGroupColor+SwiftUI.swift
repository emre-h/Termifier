// TabGroupColor+SwiftUI.swift
// Termifier
//
// SwiftUI Color extension for TabGroupColor (UI layer only).

import SwiftUI

extension TabGroupColor {
    var color: Color { Color(nsColor: nsColor) }
}
